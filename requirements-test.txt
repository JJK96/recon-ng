# Test dependencies for recon-ng
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-timeout>=2.1.0
responses>=0.22.0
freezegun>=1.2.0

# Optional: Web/API test dependencies
# Install these to run the full test suite including web tests
sanic>=23.0.0
sanic-ext>=23.0.0
sanic-testing>=23.0.0
dicttoxml
XlsxWriter
unicodecsv

# Legacy Flask dependencies (kept for reference, may be removed)
# flask
# flask-restful
# flasgger
# rq
# redis
